export const metadata = {
  title: "Flash Deals | El-Shehawy Store",
  description: "Discover exclusive flash deals on premium electronics. Grab them before they're gone!",
};

export default function DealsLayout({ children }) {
  return <>{children}</>;
}
